# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# ไฟล์สำหรับพรีวิวในเครื่องเท่านั้น (ไม่ใช้บน GitHub Pages)
_config_local.yml

# macOS
.DS_Store
**/.DS_Store

# Ruby/Bundler (กรณีใช้ภายหลัง)
Gemfile.lock
vendor/
